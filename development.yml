version: "2"
services:
  client:
    stdin_open: true
    environment:
      - CHOKIDAR_USEPOLLING=true
    # Expose the port for development purpose
    ports:
      - "3000:3000"
  api:
    # Expose the port for development purpose
    ports:
      - "4000:4000"

